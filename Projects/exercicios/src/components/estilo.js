// arquivo reservado para definir estilos 
import { StyleSheet } from 'react-native'

export default StyleSheet.create({
     // em uma mesma definição de estilos podem haver mais de um, basta que tenham nomes diferentes para poderem ser identificados quando requeridos nos componentes
     fonteG: {
          // alterar o tamanho da fonte
          fontSize: 32,

          // alinhar o texto caso quebre a linha
          textAlign: 'center'
     }
})